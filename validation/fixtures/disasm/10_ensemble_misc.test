# Ensemble — miscellaneous corpus-natural subcommands (P1.1f).
# `info hostname`, `file exists`, `namespace children` all surface as
# `::tcl::ENSEMBLE::SUB` literals through the ensemble dispatch path.
info hostname
file exists /tmp
---
  ensemble          cmd=1 src_start=205 src_end=217 ensemble=info subcommand=hostname arg_count=1
  ensemble          cmd=2 src_start=219 src_end=234 ensemble=file subcommand=exists arg_count=2
